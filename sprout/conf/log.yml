version: 1
formatters:
  simple:
    format: "[%(asctime)s -  %(levelname)s] %(name)s: %(message)s"

handlers:
  stream:
    class: logging.StreamHandler
    formatter: simple
    level: DEBUG

loggers:
  'sprout':
    handlers: [stream]
